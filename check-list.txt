ПРОВЕРКА ПОЛЯ ВВОДА ВОЗРАСТА
    Корректные значения
    Некорректные значения (за пределами валидных диапазонов или нелогичные)
    Граничные значения
    Пограничные значения
    Дробное число — формат (через запятую и через точку)
    Дробное число — округление (с кучей знаков после запятой)
    Ноль
    Один
    Пустое поле
    Очень большое число (поиск технологической границы)
    Отрицательное число
    Нечисловые значения
